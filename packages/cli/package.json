{
  "name": "xai-solutions",
  "version": "0.1.0",
  "description": "Docs + helper CLI for xAI Grok API and X API best practices",
  "type": "module",
  "bin": {
    "xai-solutions": "./bin.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "build:js": "bun build src/cli.ts --target bun --outfile dist/cli.js",
    "build:targets": "bun run scripts/build-binaries.ts",
    "build:artifacts": "bun run generate:manifest && bun run clean && mkdir -p dist && bun run build:js && bun run build:targets",
    "build:exe": "bun build src/cli.ts --compile --outfile dist/xai-solutions",
    "generate:manifest": "bun run scripts/generate-manifest.ts",
    "prepublishOnly": "bun run build:artifacts",
    "dev": "bun src/cli.ts"
  },
  "files": [
    "bin.js",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "@effect/cli": "^0.73.0",
    "@effect/platform": "^0.94.1",
    "@effect/platform-bun": "^0.87.0",
    "effect": "^3.19.14",
    "gray-matter": "^4.0.3",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.0.5",
    "bun-types": "^1.3.5",
    "typescript": "^5.7.3"
  },
  "keywords": [
    "xai",
    "grok",
    "x-api",
    "twitter",
    "typescript",
    "best-practices",
    "cli",
    "docs"
  ],
  "author": "Adam Ferguson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/adamferguson/xai-solutions.git",
    "directory": "packages/cli"
  }
}
